type Schema {
  query: Query
  mutation: Mutation
}

type Query {
  getItem(id: ID!): Item
}

type Mutation {
  createItem(input: CreateItemInput): Item
}

type Item {
  id: ID!
  foo: String
}

input CreateItemInput {
  foo: String
}